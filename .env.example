# Copy this file to `.env` and fill in real values (DO NOT commit .env)
# Example environment variables for exam-system

# Database
DB_HOST=IP
DB_PORT=PORT
DB_NAME=NAME
DB_USER=your_db_user
DB_PASSWORD=your_db_password

# Other secrets
# SENTRY_DSN=
# OAUTH_CLIENT_SECRET=
