# .ignore
# 列出项目中常见的编译/构建/生成产物，供参考。
# 注意：这个文件本身不会自动把产物移动到 `./ignore`，
# 要把生成物放到 `./ignore`，需要修改各自的构建配置（如 Maven 的 outputDirectory、webpack 的 output.path 等），
# 或手动移动产物：例如 `mv exam-api/target ignore/`、`mv exam-vue/dist ignore/`。

# Java / Maven
/target/
/**/*.class
/**/*.jar
/exam-api/target/
/exam-api/target/**
/exam-api/target/*.jar
/exam-api/target/classes/

# Node / Vue
/exam-vue/node_modules/
/exam-vue/dist/
/exam-vue/dist/**
/exam-vue/build/
/exam-vue/.temp/

# Static build outputs
/dist/
/build/

# Logs
/logs/
/logs/**
*.log
npm-debug.log*
yarn-error.log*

# IDE / OS
/.idea/
*.iml
.vscode/
.DS_Store

# Package locks (optional to ignore)
package-lock.json
yarn.lock

# Other
.ignore/

# Usage notes (中文)：
# - 如果你希望构建输出直接写入 `./ignore`，请根据各自工具修改配置：
#   - Maven（exam-api）：在 `pom.xml` 中修改 `<build><directory>` 或在插件中配置输出目录为 `${project.basedir}/ignore/exam-api-target`。
#   - Webpack（exam-vue）：在 webpack 配置或 vue.config.js 中把 `output.path` 设置为 `path.resolve(__dirname, '../ignore/exam-vue-dist')`。
# - 也可以手动在构建后移动文件到 `./ignore`，例如（Windows PowerShell）：
#   Move-Item -Path .\exam-api\target -Destination .\ignore\exam-api-target -Force
#   Move-Item -Path .\exam-vue\dist -Destination .\ignore\exam-vue-dist -Force
